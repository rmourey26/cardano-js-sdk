// TODO - Used just for fast checks
/* import { KeyManagement } from '../../src';
import { TransportType } from '../../src/KeyManagement/InMemoryLedgerKeyAgent';

describe('InMemoryLedgerKeyAgent', () => {
  let keyAgent: any;
  // let keyAgent: KeyManagement.InMemoryLedgerKeyAgent;

  beforeEach(() => {
    keyAgent = new KeyManagement.InMemoryLedgerKeyAgent();
  });

  test('accountIndex', () => {
    expect(typeof keyAgent.accountIndex).toBe("undefined");
  });

  test('networkId', () => {
    expect(typeof keyAgent.networkId).toBe("undefined");
  });

  test('initiate WebHid transport', () => {
    const transport = KeyManagement.InMemoryLedgerKeyAgent.initiateTransport({
      transportType: TransportType.WebHid
    });
    expect(transport).toBe(undefined);
  });
}); */
